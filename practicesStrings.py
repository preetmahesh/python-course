name = input("Enter your name: ")

# print("Good Afternoon", name)  # what if name is some number so we format 
                            # the string so the statemnt can be formatted with any variable value

print(f"Good Afternoon" {name})

# we can replace words/placeholders in letters and emails but actual value by using .replace
